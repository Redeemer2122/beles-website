---
type ProjectDirection = {
  slug: string;
  title: string;
  description: string;
  image: string;
  imageAlt: string;
  mediaFit: "cover" | "contain";
  mediaPosition: string;
};

type Props = {
  content: {
    eyebrow: string;
    title: string;
    titleLines?: string[];
    description: string;
    featured: {
      eyebrow: string;
      title: string;
      category: string;
      description: string;
      cta: string;
      imageAlt: string;
    };
    directions: ProjectDirection[];
  };
};

const { content } = Astro.props;
const titleLines = content.titleLines ?? [content.title];

const featuredImage = "/img/projects/projects-featured-umut12.webp";
const heroImage = "/img/projects/projects-hero-bg.webp";
---

<div class="projects-page">
  <section class="projects-hero" aria-labelledby="projects-page-title">
    <div class="projects-shell projects-hero__shell">
      <div class="projects-hero__copy">
        <p class="projects-eyebrow">{content.eyebrow}</p>
        <h1 id="projects-page-title" aria-label={content.title}>
          {titleLines.map((line) => <span aria-hidden="true">{line}</span>)}
        </h1>
        <p class="projects-hero__description">{content.description}</p>
      </div>

      <div class="projects-hero__architecture" aria-hidden="true">
        <img src={heroImage} alt="" width="1672" height="941" decoding="async" />
      </div>
    </div>
  </section>

  <section class="projects-portfolio" aria-label={content.eyebrow}>
    <div class="projects-shell">
      <article class="featured-project" id="featured-project">
        <div
          class="project-media project-media--featured"
          id="featured-project-media"
          style="--media-fit: cover; --media-position: center center;"
        >
          <img
            src={featuredImage}
            alt={content.featured.imageAlt}
            width="1672"
            height="941"
            loading="eager"
            fetchpriority="high"
            decoding="async"
          />
        </div>

        <div class="featured-project__veil" aria-hidden="true"></div>
        <div class="featured-project__content">
          <p class="projects-eyebrow">{content.featured.eyebrow}</p>
          <div class="featured-project__details">
            <p class="featured-project__category">{content.featured.category}</p>
            <h2>{content.featured.title}</h2>
            <p class="featured-project__description">{content.featured.description}</p>
            <a class="project-link" href="#featured-project-media">
              <span>{content.featured.cta}</span>
              <span class="project-link__arrow" aria-hidden="true">→</span>
            </a>
          </div>
        </div>
      </article>

      <div class="projects-grid" id="platform-directions">
        {
          content.directions.map((project) => (
            <article
              class="project-card"
              id={project.slug}
              data-media-fit={project.mediaFit}
              style={`--media-fit: ${project.mediaFit}; --media-position: ${project.mediaPosition};`}
            >
              <div class="project-media" id={`${project.slug}-media`}>
                <img
                  src={project.image}
                  alt={project.imageAlt}
                  width="1920"
                  height="1280"
                  loading="lazy"
                  decoding="async"
                />
              </div>
              <div class="project-card__veil" aria-hidden="true" />
              <div class="project-card__content">
                <h2>{project.title}</h2>
                <span class="project-card__rule" aria-hidden="true" />
                <p>{project.description}</p>
                <a class="project-link" href={`#${project.slug}-media`}>
                  <span>{content.featured.cta}</span>
                  <span class="project-link__arrow" aria-hidden="true">
                    →
                  </span>
                </a>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>
</div>

<style>
  .projects-page {
    --projects-gutter: clamp(1.5rem, 3.75vw, 4.5rem);
    --projects-border: rgba(25, 35, 47, 0.14);
    --projects-blue: #1256bc;
    --projects-ink: #18212b;
    margin-top: -4.75rem;
    overflow: clip;
    background: #f5f6f7;
    color: var(--projects-ink);
  }

  .projects-shell {
    width: 100%;
    margin-inline: auto;
    padding-inline: var(--projects-gutter);
  }

  .projects-eyebrow {
    margin: 0;
    color: var(--projects-blue);
    font-family: "Space Grotesk", var(--font-display);
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: var(--tracking-label);
    line-height: 1.2;
    text-transform: uppercase;
  }

  .projects-hero {
    position: relative;
    min-height: clamp(39rem, 47vw, 44rem);
    border-bottom: 1px solid var(--projects-border);
    background: #f5f6f7;
  }

  .projects-hero::before {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(18, 86, 188, 0.038) 1px, transparent 1px),
      linear-gradient(90deg, rgba(18, 86, 188, 0.038) 1px, transparent 1px);
    background-size: 5.5rem 5.5rem;
    content: "";
    mask-image: linear-gradient(90deg, transparent 0 38%, #000 72%);
    pointer-events: none;
  }

  .projects-hero__shell {
    position: relative;
    display: flex;
    align-items: center;
    min-height: inherit;
    padding-top: calc(4.75rem + 3.75rem);
    padding-bottom: 4.5rem;
  }

  .projects-hero__copy {
    position: relative;
    z-index: 2;
    width: min(48rem, 48%);
    min-width: 0;
  }

  .projects-hero h1 {
    display: grid;
    margin: 2.25rem 0 0;
    color: var(--projects-ink);
    font-family: Georgia, "Times New Roman", serif;
    font-size: 5.75rem;
    font-weight: 400;
    letter-spacing: 0;
    line-height: 0.92;
  }

  :global(html[lang="ky"]) .projects-hero h1 {
    font-size: 4.95rem;
  }

  .projects-hero h1 span:last-child::after {
    color: var(--projects-blue);
    content: ".";
  }

  .projects-hero__description {
    max-width: 30rem;
    margin: 2.35rem 0 0;
    padding-top: 1.1rem;
    border-top: 2px solid var(--projects-blue);
    color: rgba(24, 33, 43, 0.67);
    font-size: 1.06rem;
    line-height: 1.6;
    text-wrap: pretty;
  }

  .projects-hero__architecture {
    position: absolute;
    inset: 0;
    overflow: hidden;
    opacity: 0.78;
    pointer-events: none;
  }

  .projects-hero__architecture::after {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      90deg,
      rgba(245, 246, 247, 0.98) 0%,
      rgba(245, 246, 247, 0.76) 24%,
      rgba(245, 246, 247, 0.14) 46%,
      transparent 62%
    );
    content: "";
  }

  .projects-hero__architecture img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    filter: saturate(0.78) contrast(0.98);
  }

  .projects-portfolio {
    padding-block: 2rem clamp(6rem, 8vw, 9rem);
  }

  .featured-project,
  .project-card {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    border: 1px solid var(--projects-border);
    background: #eef1f3;
  }

  .featured-project {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.25fr);
    align-items: stretch;
    border-color: rgba(25, 35, 47, 0.18);
    box-shadow: 0 1.15rem 2.5rem rgba(24, 33, 43, 0.055);
  }

  .project-media {
    position: absolute;
    inset: 0;
    z-index: -2;
    overflow: hidden;
    background: #e2e6ea;
  }

  .project-media img {
    width: 100%;
    height: 100%;
    object-fit: var(--media-fit, cover);
    object-position: var(--media-position, center center);
    filter: saturate(0.62) contrast(1.03) brightness(1.02);
    transition: filter var(--duration-medium) var(--ease-out);
  }

  .featured-project__veil,
  .project-card__veil {
    position: absolute;
    inset: 0;
    z-index: -1;
    pointer-events: none;
  }

  .featured-project__veil {
    display: none;
  }

  .project-media--featured {
    position: relative;
    inset: auto;
    z-index: 0;
    grid-column: 2;
    grid-row: 1;
    aspect-ratio: 16 / 9;
    background: #e2e8ed;
  }

  .project-media--featured img {
    object-fit: contain;
    object-position: center;
    filter: saturate(0.76) contrast(1.03) brightness(1.01);
  }

  .featured-project__content {
    display: grid;
    grid-template-rows: auto auto;
    align-content: center;
    align-items: start;
    grid-column: 1;
    grid-row: 1;
    gap: clamp(2rem, 3.5vw, 3.5rem);
    width: 100%;
    min-width: 0;
    padding: clamp(2.5rem, 4vw, 5rem);
    background: linear-gradient(135deg, #fbfcfd 0%, #eff3f6 100%);
  }

  .featured-project__details {
    min-width: 0;
    margin: 0;
  }

  .featured-project__category {
    margin: 0;
    color: rgba(24, 33, 43, 0.54);
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  .featured-project h2 {
    max-width: 14ch;
    margin: 0.8rem 0 0;
    font-family: Georgia, "Times New Roman", serif;
    font-size: 3.35rem;
    font-weight: 400;
    letter-spacing: 0;
    line-height: 0.98;
  }

  .featured-project__description {
    max-width: 27rem;
    margin: 1.4rem 0 0;
    color: rgba(24, 33, 43, 0.68);
    font-size: 0.94rem;
    line-height: 1.65;
  }

  .project-link {
    display: inline-flex;
    align-items: center;
    justify-content: space-between;
    width: min(100%, 10.5rem);
    margin-top: 1.75rem;
    padding-bottom: 0.55rem;
    border-bottom: 1px solid var(--projects-blue);
    color: var(--projects-blue);
    font-family: "Space Grotesk", var(--font-display);
    font-size: 0.69rem;
    font-weight: 700;
    letter-spacing: var(--tracking-label);
    line-height: 1.2;
    text-decoration: none;
    text-transform: uppercase;
  }

  .project-link__arrow {
    font-size: 1.05rem;
    line-height: 0.7;
    transition: transform var(--duration-medium) var(--ease-out);
  }

  .project-link:hover .project-link__arrow,
  .project-link:focus-visible .project-link__arrow {
    transform: translateX(0.3rem);
  }

  .featured-project:hover .project-media img,
  .project-card:hover .project-media img {
    filter: saturate(0.78) contrast(1.04) brightness(1.02);
  }

  .featured-project:hover .project-media--featured img {
    filter: saturate(0.84) contrast(1.04) brightness(1.01);
  }

  .project-card#heavy-special-vehicles .project-media img {
    filter: saturate(0.48) contrast(1.04) brightness(1.03);
  }

  .project-card#electric-bus-platform .project-media img {
    filter: saturate(0.54) contrast(1.04) brightness(1.03);
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
    margin-top: 1rem;
  }

  .project-card {
    aspect-ratio: 2.22 / 1;
  }

  .project-card__veil {
    background: linear-gradient(
      90deg,
      rgba(249, 250, 251, 0.99) 0,
      rgba(249, 250, 251, 0.97) 28%,
      rgba(249, 250, 251, 0.7) 43%,
      rgba(249, 250, 251, 0.08) 66%,
      transparent 78%
    );
  }

  .project-card__content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    width: 43%;
    height: 100%;
    padding: clamp(1.65rem, 2.4vw, 2.75rem);
  }

  .project-card h2 {
    max-width: 12ch;
    margin: 0;
    font-family: Georgia, "Times New Roman", serif;
    font-size: 2rem;
    font-weight: 400;
    letter-spacing: 0;
    line-height: 1;
    text-wrap: balance;
  }

  .project-card__rule {
    width: 1.75rem;
    height: 2px;
    margin-top: 1rem;
    background: var(--projects-blue);
  }

  .project-card__content > p {
    max-width: 22rem;
    margin: 1rem 0 0;
    color: rgba(24, 33, 43, 0.68);
    font-size: 0.82rem;
    line-height: 1.55;
  }

  .project-card .project-link {
    margin-top: auto;
    padding-top: 1rem;
  }

  @media (max-width: 1180px) {
    .projects-page {
      --projects-gutter: clamp(1.5rem, 3.5vw, 3rem);
    }

    .projects-hero h1 {
      font-size: 4.75rem;
    }

    :global(html[lang="ky"]) .projects-hero h1 {
      font-size: 3rem;
    }

    .projects-hero__copy {
      width: min(48rem, 53%);
    }

    .featured-project h2 {
      font-size: 3.1rem;
    }

    .projects-grid {
      grid-template-columns: 1fr;
    }

    .project-card {
      aspect-ratio: 2.55 / 1;
    }

    .project-card__content {
      width: 38%;
    }
  }

  @media (max-width: 767px) {
    .projects-page {
      --projects-gutter: 1rem;
      margin-top: -4.1rem;
    }

    .projects-hero {
      min-height: 31rem;
    }

    .projects-hero::before {
      background-size: 3.5rem 3.5rem;
      mask-image: linear-gradient(180deg, #000, transparent 68%);
    }

    .projects-hero__shell {
      align-items: flex-end;
      padding-top: calc(4.1rem + 4rem);
      padding-bottom: 3.5rem;
    }

    .projects-hero__copy {
      width: 100%;
    }

    .projects-hero h1 {
      margin-top: 1.75rem;
      font-size: 3rem;
      line-height: 0.96;
    }

    :global(html[lang="ky"]) .projects-hero h1 {
      font-size: 2.7rem;
    }

    .projects-hero__description {
      margin: 2rem 0 0;
      padding-left: 1rem;
      font-size: 0.96rem;
    }

    .projects-hero__architecture {
      display: none;
    }

    .projects-portfolio {
      padding-block: 1rem 6rem;
    }

    .featured-project {
      display: grid;
      grid-template-columns: 1fr;
    }

    .featured-project__content {
      align-content: start;
      grid-row: 1;
      gap: 2.5rem;
      width: 100%;
      padding: 1.75rem;
      background: #fafbfc;
    }

    .featured-project h2 {
      font-size: 2.65rem;
    }

    .featured-project__veil {
      display: none;
    }

    .project-media--featured {
      position: relative;
      z-index: 0;
      grid-column: 1;
      grid-row: 2;
      aspect-ratio: 16 / 10;
    }

    .projects-grid {
      gap: 0.75rem;
      margin-top: 0.75rem;
    }

    .project-card {
      aspect-ratio: 1.12 / 1;
    }

    .project-card__veil {
      background: linear-gradient(
        90deg,
        rgba(249, 250, 251, 0.99) 0,
        rgba(249, 250, 251, 0.94) 43%,
        rgba(249, 250, 251, 0.4) 72%,
        transparent 100%
      );
    }

    .project-card__content {
      width: 66%;
      padding: 1.5rem;
    }

    .project-card h2 {
      font-size: 1.75rem;
    }

    .project-card__content > p {
      font-size: 0.78rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .project-media img,
    .project-link__arrow {
      transition: none;
    }
  }
</style>
